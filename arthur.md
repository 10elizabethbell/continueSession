# continueSession — spin-up

Say **"arthur, spin up my continueSession project"**. One Ghostty window per
plain line (opened at this directory); `@run` = headless; `@open` = macOS open.

```spinup
claude
# launch the menu-bar app (prebuilt bundle)
@run open build/continueSession.app
```
